@using System.Security.Principal
@model Lotteri.Models.LottoItemModel

@{
    ViewData["Title"] = "Details";
}

<h2>Detaljer</h2>

<div>
    <h4>Utlottning</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Name)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.DateFrom)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.DateFrom)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.DateTo)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.DateTo)
        </dd>
    </dl>
</div>
<hr />
<div>
    <form asp-action="Subscribe">
        <input type="hidden" asp-for="Id" />
        <input type="hidden" asp-for="Name" value=@User.Identity.Name />

        <input type="submit" value="Anmäl dig" class="btn btn-default" />
    </form>

    <br />
    <a asp-action="Index">Tillbaka till listan</a>
</div>
